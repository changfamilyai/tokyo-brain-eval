# Pin the lint rule set so `ruff check .` is reproducible across ruff versions.
# E4/E7/E9/F are ruff's defaults; BLE (blind except) and DTZ (naive datetimes) are added.
[lint]
select = ["E4", "E7", "E9", "F", "BLE", "DTZ"]
